Binding. Where MASTER.md disagrees, this file and docs/rewrite/07-design-system.md win.

# BCEM Energy (ekokod) — overrides to the generated MASTER.md

`MASTER.md` was generated by `ui-ux-pro-max` (F5 Task 1, plan ruling D1) and is kept verbatim: never edit
it and never re-run the skill with `--force`. Read it first, then this file. Every UI plan's Global
Constraints must say "read `MASTER.md`, then `OVERRIDES.md`".

| Area | MASTER.md says | Binding rule | Source |
|---|---|---|---|
| Category | Smart Home/IoT Dashboard | Energy management and analytics for Turkish commercial/industrial sites: precise, dense, calm | 07 §1 |
| Style | Glassmorphism: backdrop blur, translucent layers | Dashboard: flat surfaces (`surface`, `surface-raised`) with a 1 px `border` or a `shadow-md` (customiser card mode). Glass accents only on the public marketing site (F12) | 07 §10 |
| Colours | Slate `#1E293B` primary, `#22C55E` accent, dark-only `#0F172A` background, white ring | 07 §2 emerald brand/status/energy tokens, light and dark, one `light-dark()` declaration each in `web/src/styles/tokens.css`. Light `primary` darkened to `#047857` and `foreground-subtle` to `#5A6B65`; dark `foreground-subtle` `#82978F`; `#059669` survives as `brand` (graphics and logo); added `border-control`, `on-danger`, `overlay`, `card-edge` | 07 §2, plan D3/D4 |
| Colour use | — | Status is colour + icon + text. Energy colours are graphics only (never text). Forecast is always neutral and dashed | 07 §2.4 |
| Typography | Fira Code headings, Fira Sans body, Google Fonts CSS import | Lexend (headings/UI), Source Sans 3 (body), JetBrains Mono (tabular figures), self-hosted subset woff2 via `next/font/local`; no Google Fonts request | 07 §3, plan D6 |
| Type scale | — | `type-display/h1/h2/h3/body/body-lg/small/caption/metric/data` utilities (07 §3 scale); body 14 px in the dashboard, never below 13 px | 07 §3 |
| Spacing | `--space-xs..3xl` (2–32 px) | Tailwind's 4 px scale: 07 §4 `--space-N` = Tailwind `p-N`/`gap-N`. No named space tokens | 07 §4 |
| Shadows | `rgba(0,0,0,…)` including `shadow-xl` | `shadow-sm/md/lg` with `rgb(11 42 34 / …)`; transparent in dark, where `card-edge` draws the edge. No `shadow-xl` | 07 §4 |
| Radius | 8–16 px literals | `rounded-sm/md/lg/full` = 4/8/12 px × `--radius-scale` (customiser 0.5–1.5) | 07 §4, plan D15 |
| Buttons | Hover `translateY(-1px)`, `transition: all 200ms`, 12×24 padding | No hover lift; colour transition at `--duration-hover` (150 ms); sizes sm 32 / md 36 / lg 44 px; 44×44 under `pointer: coarse` | 07 §8, plan D13 |
| Cards | Hover lift, `cursor: pointer` on every card | Cards are static containers; only interactive elements get a pointer | 07 §8 |
| Inputs | `outline: none` with a box-shadow focus | Focus rings are global (`:focus-visible` 2 px `ring`, 2 px offset); `outline-none` is a lint error; control border `border-control` (3:1) | 07 §9, plan D5 |
| Modals | `backdrop-filter: blur(4px)`, 16 px radius, white background | `overlay` scrim with no blur; `surface-raised`, `rounded-lg`; 250 ms in / 180 ms out fade + scale | 07 §8, §10 |
| Motion | GSAP `back.out(1.4)` stagger | CSS transitions/keyframes only, duration tokens from `tokens.css`; no GSAP, no overshoot; animate transform/opacity; `prefers-reduced-motion` resolves immediately. Card-grid stagger uses `--duration-stagger-step` (F6) | 07 §8, plan D26 |
| Page pattern | Real-Time / Operations Landing (hero, CTA) | Dashboard shell of 07 §7. The landing pattern applies only to the public site (F12) | 07 §7, §10 |
| Anti-patterns / checklist | Generic list | Use 07 §11 as the pre-delivery checklist; MASTER's list is a subset of it | 07 §11 |
