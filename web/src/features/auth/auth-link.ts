/** Secondary text links on auth pages; 44 px tall under a coarse pointer (plan D13). */
export const authLinkClass =
  'rounded-sm text-primary underline-offset-2 type-small hover:underline ' +
  'pointer-coarse:inline-flex pointer-coarse:min-h-11 pointer-coarse:items-center';
